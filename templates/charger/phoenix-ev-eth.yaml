type: t_phoenix-ev-eth
description: Phoenix EV-ETH Controller (Modbus TCP)
params:
- name: host
  required: true
  example: 192.0.2.2
- name: port
  default: 502
render: |
  type: phoenix-ev-eth
  uri: {{ .host }}:{{ .port }}
  meter: # only if a charge meter is connected to the controller
    power: true
    energy: true
    currents: true