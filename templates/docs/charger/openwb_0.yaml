product:
  brand: openWB
  description: series2
description: |
  Die Wallbox muss als Ladepunkt konfiguriert sein.
render:
  - default: |
      type: template
      template: openwb
      host: 192.0.2.2 # IP-Adresse oder Hostname
    advanced: |
      type: template
      template: openwb
      host: 192.0.2.2 # IP-Adresse oder Hostname
      connector: 1 # Normalerweise 1 für den ersten Anschluss. # Optional
      phases1p3p: false # Optional
