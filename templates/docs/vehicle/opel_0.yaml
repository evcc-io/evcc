product:
  brand: Opel

render:
- default: |
    type: template
    template: opel
    title: # Optional 
    user:
    password:
    vin: WP0...  # Optional 
    capacity: 50  # Optional 
  advanced: |
    type: template
    template: opel
    title: # Optional 
    user:
    password:
    vin: WP0...  # Optional 
    capacity: 50  # Optional 
    cache: # Optional 
    mode: # Optional 
    minSoC: # Optional 
    targetSoC: # Optional 
    minCurrent: # Optional 
    maxCurrent: # Optional 
    identifiers: # Optional