product:
  brand: Ford

render:
- default: |
    type: template
    template: ford
    title: # Optional 
    user:
    password:
    vin: WF0FXX...  # Optional 
    capacity: 10  # Optional 
  advanced: |
    type: template
    template: ford
    title: # Optional 
    user:
    password:
    vin: WF0FXX...  # Optional 
    capacity: 10  # Optional 
    cache: # Optional 
    mode: # Optional 
    minSoC: # Optional 
    targetSoC: # Optional 
    minCurrent: # Optional 
    maxCurrent: # Optional 
    identifiers: # Optional