product:
  brand: Renault

render:
- default: |
    type: template
    template: renault
    title: # Optional 
    user:
    password:
    vin: WREN...  # Optional 
    capacity: 60  # Optional 
  advanced: |
    type: template
    template: renault
    title: # Optional 
    user:
    password:
    vin: WREN...  # Optional 
    capacity: 60  # Optional 
    cache: # Optional 
    mode: # Optional 
    minSoC: # Optional 
    targetSoC: # Optional 
    minCurrent: # Optional 
    maxCurrent: # Optional 
    identifiers: # Optional