product:
  brand: Skoda
  description: Enyaq

render:
- default: |
    type: template
    template: enyaq
    title: # Optional 
    user:
    password:
    vin: W...  # Optional 
    capacity: 50  # Optional 
  advanced: |
    type: template
    template: enyaq
    title: # Optional 
    user:
    password:
    vin: W...  # Optional 
    capacity: 50  # Optional 
    cache: # Optional 
    mode: # Optional 
    minSoC: # Optional 
    targetSoC: # Optional 
    minCurrent: # Optional 
    maxCurrent: # Optional 
    identifiers: # Optional