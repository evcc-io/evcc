product:
  brand: Dacia

render:
- default: |
    type: template
    template: dacia
    title: # Optional 
    user:
    password:
    vin: W...  # Optional 
    capacity: 27.4  # Optional 
  advanced: |
    type: template
    template: dacia
    title: # Optional 
    user:
    password:
    vin: W...  # Optional 
    capacity: 27.4  # Optional 
    cache: # Optional 
    mode: # Optional 
    minSoC: # Optional 
    targetSoC: # Optional 
    minCurrent: # Optional 
    maxCurrent: # Optional 
    identifiers: # Optional