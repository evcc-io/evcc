product:
  brand: Seat
  description: Cupra

render:
- default: |
    type: template
    template: seat
    title: # Optional 
    user:
    password:
    vin: W...  # Optional 
    capacity: 10  # Optional 
  advanced: |
    type: template
    template: seat
    title: # Optional 
    user:
    password:
    vin: W...  # Optional 
    capacity: 10  # Optional 
    cache: # Optional 
    mode: # Optional 
    minSoC: # Optional 
    targetSoC: # Optional 
    minCurrent: # Optional 
    maxCurrent: # Optional 
    identifiers: # Optional