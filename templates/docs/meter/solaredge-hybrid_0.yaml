product:
  brand: SolarEdge
  description: Hybrid Inverter
description: |
  Nur ein System kann auf den Wechselrichter zugreifen!
render:
  - usage: grid
    default: |
      type: template
      template: solaredge-hybrid
      usage: grid
      host: 192.0.2.2 # IP-Adresse oder Hostname
      port: 502 # Port 502 (SetApp) oder 1502 (LCD) # Optional
      timeout: 10s # Optional
  - usage: pv
    default: |
      type: template
      template: solaredge-hybrid
      usage: pv
      host: 192.0.2.2 # IP-Adresse oder Hostname
      port: 502 # Port 502 (SetApp) oder 1502 (LCD) # Optional
      timeout: 10s # Optional
  - usage: battery
    default: |
      type: template
      template: solaredge-hybrid
      usage: battery
      host: 192.0.2.2 # IP-Adresse oder Hostname
      port: 502 # Port 502 (SetApp) oder 1502 (LCD) # Optional
      timeout: 10s # Optional
