template: id
description: Volkswagen (We Connect ID)
loglevel: id
paramsbase: vehicle
params:
- name: vin
  example: WVWZZZ...
- name: capacity
  default: 50
render: |
  type: id
  {{- if ne .title "" }}
  title: {{ .title }}
  {{- end }}
  user: {{ .user }}
  password: '{{ .password }}'
  {{- if ne .capacity "" }}
  capacity: {{ .capacity }}
  {{- end }}
  {{- if ne .vin "" }}
  vin: {{ .vin }}
  {{- end }}
