type: t_ds
description: DS
params:
- name: title
- name: user
  required: true
- name: password
  required: true
  mask: true
- name: vin
  example: W...
- name: capacity
  default: 50
render: |
  type: ds
  {{- if ne .title "" }}
  title: {{ .title }}
  {{- end }}
  user: {{ .user }}
  password: '{{ .password }}'
  capacity: {{ .capacity }}
  {{- if ne .vin "" }}
  vin: {{ .vin }}
  {{- end }}
