template: voltie
products:
  - brand: Voltie
    description:
      generic: Charger
capabilities: ["mA"]
requirements:
  description:
    en: The Voltie charger must be configured to operate in Modbus slave mode. This is done either via the mobile application or by the backend server upon user's request.
    de: Der Voltie-Ladegerät muss so konfiguriert werden, dass es im Modbus-Slave-Modus arbeitet. Dies geschieht entweder über die mobile Anwendung oder durch den Backend-Server auf Anfrage des Benutzers.
  evcc: ["sponsorship"]
params:
  - name: modbus
    choice: ["tcpip"]
    id: 1
render: |
  type: voltie
  {{- include "modbus" . }}
