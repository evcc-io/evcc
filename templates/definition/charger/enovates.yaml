template: enovates
products:
  - brand: Enovates
  - brand: Shell Recharge
    description:
      generic: Home Advanced
capabilities: ["mA", "meter"]
requirements:
  description:
    de: "EMS über Modbus TCP muss auf der Ladestation aktiviert sein (ModBusTCPEMSEnabled und modbus.tcp.enabled = true)."
    en: "EMS over Modbus TCP must be enabled on the charger (ModBusTCPEMSEnabled and modbus.tcp.enabled = true)."
  evcc: ["sponsorship"]
params:
  - name: modbus
    choice: ["tcpip"]
    id: 1
render: |
  type: enovates
  {{- include "modbus" . }}
