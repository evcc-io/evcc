template: cfos
products:
  - brand: cFos
    description:
      generic: PowerBrain
requirements:
  description:
    de: Bei cFos PowerBrain charger muss das [`charge meter`](meters) separat konfiguriert werden.
    en: For cFos PowerBrain charger, the [`charge meter`](meters) must be configured separately.
  evcc: ["sponsorship"]
params:
  - name: host
render: |
  type: cfos
  uri: {{ .host }}
