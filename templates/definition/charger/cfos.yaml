template: cfos
products:
  - brand: cFos
    description:
      generic: PowerBrain
requirements:
  evcc: ["sponsorship"]
params:
  - name: host
    required: true
    example: 192.0.2.2
render: |
  type: cfos
  uri: {{ .host }}
