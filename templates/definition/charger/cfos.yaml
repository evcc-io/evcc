template: cfos
products:
  - brand: cFos
    description:
      generic: PowerBrain
requirements:
  description:
    de: Bei cFos PowerBrain charger muss der cFos Zähler separat als Ladezähler konfiguriert werden [`charge meter`](meters).
    en: For cFos PowerBrain charger, cFos meter must be configured separately als charge meter [`charge meter`](meters).
  evcc: ["sponsorship"]
params:
  - name: host
render: |
  type: cfos
  uri: {{ .host }}
