template: elliconnect
products:
  - brand: Elli
    description:
      de: Charger Connect (ohne Meter)
      en: Charger Connect (without Meter)
capabilities: ["mA"]
requirements:
  evcc: ["eebus"]
  description:
    de: Dem Gerät muss eine feste IP Adresse zugewiesen sein (Manuell oder per DHCP). Ein Stromzähler wird benötigt.
    en: The device has to have a fix IP address (manuall or via DHCP). Also a meter is required.
params:
  - preset: eebusnometer
  - name: ip
    required: true
render: |
  {{ include "eebus-no-meter" . }}
