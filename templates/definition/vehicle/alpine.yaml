template: myalpine
products:
  - brand: Alpine
params:
  - preset: vehicle-base
  - preset: vehicle-features
auth:
  type: renault
  params: [user, password]
render: |
  type: myalpine
  {{ include "vehicle-base" . }}
  {{ include "vehicle-features" . }}
