template: fiat
description: Fiat
paramsbase: vehicle
params:
- name: pin
- name: vin
  example: ZFAE...
- name: capacity
  default: 42
render: |
  type: fiat
  {{include "renderbase-vehicle" .}}
  {{- if ne .pin "" }}
  pin: {{ .pin }} #mandatory to deep refresh SoC
  {{- end }}
