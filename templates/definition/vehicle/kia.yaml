template: kia
description: Kia
paramsbase: vehicle
params:
- name: capacity
  default: 64
render: |
  type: kia
  {{- if ne .title "" }}
  title: {{ .title }}
  {{- end }}
  user: {{ .user }}
  password: {{ .password }}
  capacity: {{ .capacity }}
