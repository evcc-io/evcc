template: huawei-smartlogger
products:
  - brand: Huawei
    description:
      generic: SmartLogger
requirements:
  description:
    de: |
      Die Abregelung begrenzt die Einspeiseleistung auf den gewünschten Prozentsatz der maximalen AC-Leistung. Der Wechselrichter begrenzt automatisch die PV-Leistung, um dies zu erreichen.
    en: |
      Curtailment will limit the export power to the desired percentage of the maximum AC power. The inverter will automatically limit the PV power to achieve this.
params:
  - name: modbus
    choice: ["tcpip"]
render: |
  type: custom
  curtail:
    source: modbus
    {{- include "modbus" . | indent 2 }}
    register:
      address: 40428 # Active power [–100%, 100%] - a negative value indicates the limit for active power supplied from the grid to the ESS
      type: writesingle
      encoding: int16
    scale: 10
  curtailed:
    source: go
    in:
    - name: limit
      type: int
      config:
        source: modbus
        {{- include "modbus" . | indent 6 }}
        register:
          address: 40428 # Active power [–100%, 100%] - a negative value indicates the limit for active power supplied from the grid to the ESS
          type: holding
          decode: int16
        scale: 0.1
    script: |
      percent := int(limit)
      if percent < 0 || percent > 100 {
        percent = 100
      }
      percent
