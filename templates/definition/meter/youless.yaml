template: youless
products:
  - brand: Youless
    description:
      generic: Energy Monitor
requirements:
  description:
    de: |
      Zur Erfassung der PV-Produktion ein exter angebundenener S0-Erzeugungszähler benötigt.
    en: |
      An externally connected S0 generation meter is required to record the PV production.
params:
  - name: usage
    choice: ["grid", "pv"]
  - name: host
    required: true
render: |
  type: custom
  {{- if eq .usage "grid" }}
  power:
    source: http
    uri: http://{{ .host }}/e
    method: GET
    jq: .[0]|.pwr
  energy:
    source: http
    uri: http://{{ .host }}/e
    method: GET
    jq: .[0]|.p1+.p2
  {{- end }}
  {{- if eq .usage "pv" }}
  power:
    source: http
    uri: http://{{ .host }}/e
    method: GET
    jq: .[0].ps0
  energy:
    source: http
    uri: http://{{ .host }}/e
    method: GET
    jq: .[0].cs0
  {{- end }}