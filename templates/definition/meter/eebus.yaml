template: eebus-meter
products:
  - brand: Generic
    description:
      de: EEBus Netzanschlusspunkt (MGCP)
      en: EEBus grid connection point (MGCP)
  - brand: Generic
    description:
      de: EEBus Verbraucher oder Messstelle (MPC)
      en: EEBus consumer or metering device (MPC)
requirements:
  description:
    de: Ein EEBus Verbraucher oder Messstelle. Unterstützt werden die Use Cases MGCP (Monitoring of Grid Connection Point), MPC (Monitoring & Power Consumption) und LPC (Limitation of Power Consumption).
    en: An EEBus consumer or metering device. Supports MGCP (grid connection points), MPC (generic power monitoring), and LPC (Limitation of Power Consumption) use cases.
  evcc: ["eebus"]
params:
  - name: usage
    required: false
    choice: ["grid", "aux", "ext"]
    description:
      en: Usage type (grid=MGCP, default/others=MPC)
      de: Verwendungsart (grid=MGCP, Standard/andere=MPC)
    help:
      en: |
        Optional. Determines the EEBus use case:
        - grid: MGCP (Monitoring of Grid Connection Point)
        - other: MPC (Monitoring & Power Consumption)
      de: |
        Optional. Bestimmt den EEBus Anwendungsfall:
        - grid: MGCP (Monitoring of Grid Connection Point)
        - andere: MPC (Monitoring & Power Consumption)
  - name: ski
    description:
      en: SKI (Subject Key Identifier) of the EEBus device
      de: SKI (Subject Key Identifier) des EEBus-Geräts
  - name: host
    required: false
    help:
      en: If not specified, the device will be discovered automatically via mDNS.
      de: Falls nicht angegeben, wird das Gerät automatisch über mDNS gefunden.
render: |
  type: eebus
  ski: {{ .ski }}
  {{- if .host }}
  ip: {{ .host }}
  {{- end }}
  {{- if .usage }}
  usage: {{ .usage }}
  {{- end }}
