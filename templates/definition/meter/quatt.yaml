template: quatt
products:
  - brand: Quatt
    description:
      generic: Hybrid Heat Pump
group: heating
params:
  - name: usage
    choice: ["charge"]
  - name: host
  - name: port
    default: 8080
  - name: unit
    deprecated: true
render: |
  type: quatt
  host: {{ .host }}
  port: {{ .port }}
  {{- if .unit }}
  unit: {{ .unit }}
  {{- end }}
