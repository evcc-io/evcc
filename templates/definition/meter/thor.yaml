template: THOR Heizstab
products:
  - brand: Thor
params:
  - name: usage
    choice: ["aux"]
  - name: host
render: |
  type: custom
  power:
    source: http
    uri: http://{{ .host }}/data.jsn
    jq: .power_act
