template: email
products:
  - brand: Generic
    description:
      generic: Email (via Shoutrrr)
params:
  - name: uri
    required: true
    example: smtp://username:password@smtp.example.com:587/?fromAddress=from@example.com&toAddresses=to@example.com
    help:
      de: Shoutrrr URI für E-Mail Versand (siehe https://containrrr.dev/shoutrrr/v0.8/services/email/)
      en: Shoutrrr URI for email sending (see https://containrrr.dev/shoutrrr/v0.8/services/email/)
render: |
  type: email
  uri: {{ .uri }}
