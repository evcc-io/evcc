template: amber
products:
  - brand: Amber Electric (AU)
params:
  - preset: tariff-base
  - name: token
  - name: siteid
  - name: channel
render: |
  type: amber
  {{ include "tariff-base" . }}
  token: {{ .token }}
  siteid: {{ .siteid }}
  channel: {{ .channel }}
