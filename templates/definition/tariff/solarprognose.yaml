template: solarprognose
products:
  - brand: Solarprognose
requirements:
  description:
    en: "[solarprognose.de](https://www.solarprognose.de) can be used for free (but donations are welcome). An user account is required."
    de: "[solarprognose.de](https://www.solarprognose.de) kann kostenlos verwendet werden (Spenden sind allerdings willkommen). Ein Benutzer-Account ist notwendig."
  evcc: ["skiptest"]
group: solar
params:
  - name: token
    description:
      en: Access Token from User Profile
      de: Zugriffs-Schlüssel aus Benutzer-Profil
    required: true
  - name: item
    choice: ["location", "plant", "inverter", "module_field"]
    description:
      en: Item type to be queried.
      de: Elementtyp, der abgefragt werden soll.
    help:
      en: Item and id/token are only required if more than one location is configured or if a specific item is to be queried. Otherwise, the API returns the data for the first location. See also [solarprognose.de](https://www.solarprognose.de/web/en-en/solarprediction/page/api)
      de: Item und id/token werden nur benötigt, wenn mehr als ein Standort konfiguriert ist oder ein bestimmtes Element abgefragt werden soll. Ansonsten gibt die API die Daten für den ersten Standort zurück. Siehe auch [solarprognose.de](https://www.solarprognose.de/web/de-de/solarprediction/page/api)
    advanced: true
  - name: id
    description:
      en: Unique ID of the item to be queried
      de: Eindeutige ID des abzufragenden Elements
    advanced: true
  - name: uniquetoken
    description:
      en: Unique token of the item to be queried
      de: Eindeutiger Schlüssel des abzufragenden Elements
    advanced: true
  - name: algorithm
    choice: ["mosmix", "own-v1", "clearsky"]
    description:
      en: Forecasting Algorithm (mosmix, own-v1 or clearsky)
      de: Prognosealgorithmus (mosmix, own-v1 oder clearsky)
    advanced: true
  - name: forecast_days
    description:
      en: Number of additional days to forecast (1-6)
      de: Anzahl zusätzlicher Tage für die Prognose (1-6)
    help:
      en: "Number of additional days to forecast (1-6), excluding today. Remember: evcc calculates energy for today, tomorrow, and the day after tomorrow, effectively handling up to three days of solar forecast data, but the UI shows up to 96 hours."
      de: "Anzahl der zusätzlichen Tage für die Prognose (1-6), ohne den heutigen Tag. Bedenke: evcc berechnet die Energie für heute, morgen und übermorgen und verarbeitet dabei effektiv bis zu drei Tage Solarprognosedaten, zeigt jedoch bis zu 96 Stunden an."
    default: 4
    advanced: true
  - name: project
    description:
      en: Project contact (website or email address)
      de: Projekt-Kontakt (Website oder E-Mail-Adresse)
    help:
      en: Project website or email address so that the solarprognose.de provider can contact you if necessary.
      de: Projekt-Website oder E-Mail-Adresse, damit der solarprognose.de-Provider im Bedarfsfall Kontakt aufnehmen kann.
    advanced: true
  - name: interval
    default: 1h
    advanced: true
render: |
  type: custom
  tariff: solar
  forecast:
    source: http
    # Base URL with required params (format, type, access token), then optional item-based parameters
    uri: https://www.solarprognose.de/web/solarprediction/api/v1?_format=json&type=hourly&access-token={{ .token }}{{ if .item }}&item={{ .item }}{{ if .id }}&id={{ .id }}{{ else }}&token={{ unquote .uniquetoken }}{{ end }}{{ end }}{{ if .algorithm }}&algorithm={{ .algorithm }}{{ end }}{{ if .project }}&project={{ .project }}{{ end }}{{ if .forecast_days }}&end_day={{ .forecast_days }}{{ end }}
    jq: |
      def step: 3600; # hourly interval

      def lookup:
        .data
        | to_entries
        | map({ key, value: (.value[0] * 1000) }) # convert kW to W
        | from_entries;

      . as $root
      | ($root | lookup) as $L
      | ($L | keys | map(tonumber) | sort) as $ts
      | ($ts[0]) as $min
      | ($ts[-1]) as $max
      | [ range($min; $max + step; step) as $t
          | {
              start: ($t | strftime("%FT%TZ")),
              end:   (($t + step) | strftime("%FT%TZ")),
              value: ($L[($t|tostring)] // 0)
            }
        ]
      | tostring
  interval: {{ .interval }}
