run:
  go: "1.21"
linters:
  enable:
    - forcetypeassert
issues:
  max-same-issues: 0
  exclude:
    - .regActualCurrent. is unused
    - "`routeLogger` is unused"
linters-settings:
  staticcheck:
    checks:
      - "-SA1019"
